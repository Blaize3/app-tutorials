{
  "name": "app-tutorials",
  "version": "1.0.0",
  "description": "Creating web app from scratch by Kelvin Powell",
  "scripts": {
    "start": "concurrently \"npm run gulp\" \"npm run lite\"",
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp watch"
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1",
    "lite-server": "^2.3.0"
  }
}
